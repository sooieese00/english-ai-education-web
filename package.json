{
  "name": "engweb-root",
  "version": "1.0.0",
  "description": "원하는 유튜브 영상을 가져와 영어 공부를 하고, 딥보이스 기능을 이용해 쉐도잉 연습을 할 수 있는 웹 서비스입니다.",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run start:client\" \"npm run start:backend\"",
    "start:client": "cd client && npm start",
    "start:backend": "cd engweb-backend && npm start",
    "install:all": "npm install && cd client && npm install && cd ../engweb-backend && npm install"
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}
